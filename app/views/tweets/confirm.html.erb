<h1>投稿確認画面</h1>

<%= form_with(model: @tweet, local: true, url: tweets_path) do |f| %>
  <%= f.hidden_field :content %>
  
  <h2>投稿内容</h2>
  <p class="show"><%= @tweet.content %></p>
  <p><%= f.submit "投稿する" %></p>
<% end %>
  
<%= form_with(model: @tweet, local: true, url: new_tweet_path, method: :get) do |f| %>
   <%= f.hidden_field :content %>
   <p><%= f.submit "戻る", name: "back" %></p>
<% end %>